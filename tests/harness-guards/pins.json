{
  "schema": 2,
  "captured_at": "2026-08-05",
  "source_head": "9589c5c65a0baca02a9436b3f472929531e91a88",
  "note": "Written BEFORE the genericising substitution, deliberately. Every quoted_source_string is verbatim from the read-only source at source_head (project-specific and vendor tokens redacted in angle brackets, including two vendor-named source_file paths; source_head + source_line + the quoted string still locate every entry exactly). literal/regex entries become mechanical assertions against expected_in. semantic-manual entries CANNOT be asserted mechanically because the string itself must change; they are hand-walked, and the walk is recorded in tests/harness-guards/semantic-discharges.md, which is COMMITTED. READ discharged_in AS FOLLOWS: a non-null value names where the concept landed; null means the entry is GENUINELY OPEN, never merely unrecorded, and a null entry carries partial_discharge saying what has landed and what has not. That invariant is enforced by tests/harness-guards/pins-discharge.bats. It exists because the discharge record was first written to a gitignored path: the shipped tree then asserted 23 obligations whose proof no reader could reach, and one of them (collector-single-endpoint-must-merge-both) had been recorded as discharged while half of it was missing. ADDENDUM (ci-health-watch.yml): inventory row 27 was skipped in the original capture pass, so its ten entries below were quoted from the SAME read-only source at the SAME source_head but AFTER its generic counterpart was written. Their `pattern`s were therefore chosen to fit text that already existed, which the earlier entries' were not: they guard against a FUTURE regression, and they are not evidence that the extraction itself preserved anything. Do not read them as blind captures. SCHEMA 2 (2026-08-08) ADDS SUPERSESSION. A pinned lesson can be REVERSED by later evidence — the system that taught it keeps running and keeps learning. Schema 1 had no way to say so, so the first reversal was recorded by rewriting the entry's `why`, which loses the original reasoning and reads to the next person as though the lesson had always said that. An entry may now carry `superseded_on` (ISO date), `superseded_by` (where the new decision is written down) and `superseded_reason` (what changed and why). `why` KEEPS ITS ORIGINAL TEXT — the reason a rule existed is the most valuable thing to preserve when reversing it, because it is the cost you are choosing to pay. The `pattern` of a superseded entry is NOT relaxed: a superseded lesson is one whose conclusion changed, not one that stopped mattering, and the string usually survives in a new role (see referee-sorts-does-not-grade, where the pinned phrase is now the objection being ANSWERED). Deleting a pin remains the only way to say a lesson no longer applies at all, and it is still not something to do quietly. Enforced by pins-schema.bats and surfaced in the generated suite's failure message by gen-pin-tests.sh.",
  "pins": [
    {
      "id": "review-model-pinned-not-floating-alias",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 13,
      "quoted_source_string": "#     the floating `<MODEL-ALIAS>` alias, which silently resolves to whatever the",
      "why": "A reviewing agent must be pinned to an exact model identifier, not a floating alias. A floating alias silently resolves to whatever the platform default happens to be that week, so the thing doing the judging changes underneath you with no diff, no announcement and no way to reproduce an earlier review.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "The model that performs a judgement task is selected by an exact, versioned identifier and the reason for pinning is written down next to it.",
      "replacement_must_preserve": "An explicit model/agent selection (however the generic runner spells it) plus a comment stating that a floating or default alias is not acceptable for a judgement role because it drifts silently.",
      "discharged_in": ".github/workflows/review.yml — reviewer A step comment: the model is taken from `models.judge` and pinned by exact id, with the drift reason stated next to it. See tests/harness-guards/semantic-discharges.md row 1."
    },
    {
      "id": "review-standards-doc-paths",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 130,
      "quoted_source_string": "Judge the diff against this repo's own standards, not generic ones: <STANDARDS-DOC> at the root, and <STANDARDS-DIR> -",
      "why": "A reviewer must be pointed at the repository's own written standards rather than left to invent generic ones, and those standards must be named by path so the instruction is checkable. Which paths they are is project-specific; that there ARE named paths is the lesson.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Reviewer instructions cite the repository's own standards documents by path instead of relying on the model's general taste.",
      "replacement_must_preserve": "A reference to the template's own standards files by path, and the phrase distinguishing the repo's own standards from generic ones.",
      "discharged_in": ".github/workflows/review.yml — same comment block: the reviewer is anchored to this repository's own standards by path (AGENTS.md, docs/runbooks/), not to generic best practice. See tests/harness-guards/semantic-discharges.md row 2."
    },
    {
      "id": "review-lone-finding-is-the-point",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 44,
      "quoted_source_string": "# the same model, and a finding only one of them raised is exactly the finding a",
      "why": "The entire value of a second, differently-failing reviewer is the finding only one of them raised. Any downstream step that treats disagreement as noise, or silence from one reviewer as a veto on the other, destroys the reason the second reviewer exists.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "a finding only one of them raised"
    },
    {
      "id": "referee-sorts-does-not-grade",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 55,
      "quoted_source_string": "#     the human decides. Anything else would be one party grading its own paper.",
      "why": "The comparison step runs on the same model family as one of the reviews it is comparing, so it is a party to the dispute and may only sort findings, never rule on them. A comparator that picks winners is the first reviewer marking its own work, and the human loses the disagreement that was the point.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "grading its own paper",
      "superseded_on": "2026-08-08",
      "superseded_by": "docs/runbooks/multi-model-review.md — \"The referee settles disagreements\"; guarded by tests/harness-guards/referee-verdict.bats",
      "superseded_reason": "Abstaining cost more than it saved. Every disagreement became an operator decision, and most were not disagreements at all — one reviewer's silence, the same defect at two severities, two valid fixes for one bug. The genuine ones were nearly always questions of FACT about the code, which have an answer anyone can check. The comparator now rules, and the self-grading risk this entry names is answered STRUCTURALLY rather than by abstaining: it settles questions of fact against a diff pinned to the reviewed commit, a ruling in its own side's favour requires quoted file:line evidence, a tie goes to the other reviewer, and every verdict is advice the author overrules at merge time. The pinned phrase must survive as the objection being ANSWERED — if it disappears, the reason the safeguards exist has gone with it."
    },
    {
      "id": "review-triggers-opened-and-ready-for-review",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 67,
      "quoted_source_string": "    types: [opened, ready_for_review]",
      "why": "The automatic review fires on pull request open and on draft-to-ready only. Adding push-driven re-review turns one review per pull request into one per push and, combined with an agent that pushes fixes, closes a loop that runs away on a single runner.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "types:[[:space:]]*\\[[[:space:]]*opened,[[:space:]]*ready_for_review[[:space:]]*\\]"
    },
    {
      "id": "review-never-triggers-on-synchronize",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 154,
      "quoted_source_string": "      # `ready_for_review`, never on `synchronize`, so the Steward's fix push",
      "why": "Never triggering on the push/synchronize event is what makes the fixer's push unable to bounce back into a fresh review. The comment is load-bearing: without the reason written down, adding the event later looks like an obvious improvement.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "never on .synchronize."
    },
    {
      "id": "review-concurrency-per-pr-number",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 77,
      "quoted_source_string": "${{ github.event.pull_request.number }}",
      "why": "The review concurrency group is scoped per pull request number. A single global group means work on one pull request evicts queued work on an unrelated one, and the platform gives no way to ask for a deeper queue.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "group:.*github\\.event\\.pull_request\\.number"
    },
    {
      "id": "review-concurrency-cancel-in-progress-true",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 78,
      "quoted_source_string": "  cancel-in-progress: true",
      "why": "An advisory review that is not a required status check may be cancelled by a newer run on the same pull request, because nobody reads a superseded commit's review. This is the opposite setting from the queue that must never drop a request, and the two must not be conflated.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "cancel-in-progress:[[:space:]]*true"
    },
    {
      "id": "referee-window-not-recomputed",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 87,
      "quoted_source_string": "      # Recomputing \"now\" in the referee would silently exclude the two reviews",
      "why": "The later comparison job must reuse the exact timestamp the first job recorded, exported as a job output, rather than computing its own. A freshly computed instant is after both reviews were posted, so the comparison window excludes the very comments it exists to compare, and reports nothing while going green.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "Recomputing \"now\" in the referee"
    },
    {
      "id": "review-silent-reviewer-worse-than-none",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 123,
      "quoted_source_string": "          # A reviewer that reviews and stays silent is worse than no reviewer:",
      "why": "An agent that performs a review and prints it to the run log instead of posting it produces a green check that reads as reviewed-and-clean. The posting flag is what makes the review reach the pull request; the comment recording that is why nobody removes the flag as redundant.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "A reviewer that reviews and stays silent is worse than no reviewer"
    },
    {
      "id": "review-fix-review-loop-guard-rationale",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 142,
      "quoted_source_string": "      # review->fix->review loop running away on the one self-hosted runner.",
      "why": "The bot-sender exclusion on the fixer workflow is what stops a review triggering a fix triggering a review without end. The exclusion looks like an accident until this sentence is next to it, and removing it is a one-character change.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "review->fix->review loop"
    },
    {
      "id": "handoff-files-issue-not-comment",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 149,
      "quoted_source_string": "      # `issues: [opened]` is the ONE auto-invoke path in <STEWARD-WORKFLOW> that",
      "why": "The reviewer cannot reach the fixer by commenting, because the fixer ignores every comment sent by a bot and the reviewer posts as a bot. Opening an issue is the one path that reaches the fixer without weakening the loop guard, so the handoff files an issue rather than commenting.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Cross-workflow handoff from a read-only reviewer to a write-capable fixer travels by filing an issue, precisely because the comment path is deliberately unreachable for bot senders.",
      "replacement_must_preserve": "The issue-filing handoff, plus a comment naming the newly-opened-issue trigger as the only auto-invoke path that carries no sender check, and stating that this is what keeps the loop guard intact.",
      "discharged_in": ".github/workflows/review.yml — the handoff step files an issue because `issues: [opened]` is the one steward trigger with no bot-sender check, which is stated inline with the loop-guard reasoning. See tests/harness-guards/semantic-discharges.md row 3."
    },
    {
      "id": "review-handoff-not-cancelled-not-always",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 159,
      "quoted_source_string": "        # NOT always(): that also fires when the run was CANCELLED, and on a",
      "why": "A post-run detector must be gated on not-cancelled rather than always. Deliberate cancellation is routine on a constrained runner, and an always-gated detector reports every cancellation as a lost result, which trains the reader to ignore the one warning that matters.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "NOT always\\(\\)"
    },
    {
      "id": "handoff-pat-required-default-token-inert",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 170,
      "quoted_source_string": "          # does not start workflow runs from events created with GITHUB_TOKEN,",
      "why": "Events created with the default workflow token do not start further workflow runs. An issue filed with it is visible but inert: it wakes nobody. Any automation that hands work to another workflow by creating an event needs an elevated token, or it is a no-op that looks like success.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "does not start workflow runs from events created with GITHUB_TOKEN"
    },
    {
      "id": "handoff-warns-loudly-when-elevated-token-absent",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 395,
      "quoted_source_string": "            echo \"::warning::Handoff issue filed with GITHUB_TOKEN - it will NOT invoke the Steward. Set <HANDOFF-TOKEN-SECRET>.\"",
      "why": "When the elevated token is missing the handoff still files the issue, but it says so loudly in the run log and in the issue body itself. Nothing may quietly read as handed off when it was not; a visible finding beats a lost one, and a silent degradation is indistinguishable from success.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Degradation to the non-triggering default token is announced in both channels a human might read, rather than being absorbed silently.",
      "replacement_must_preserve": "A workflow-log warning annotation AND an in-issue warning block, both stating that the handoff did not invoke the downstream workflow and naming the secret that would fix it.",
      "discharged_in": ".github/workflows/review.yml — a `::warning::` annotation AND a `> [!WARNING]` block in the issue body, both naming STEWARD_HANDOFF_PAT. See tests/harness-guards/semantic-discharges.md row 4."
    },
    {
      "id": "handoff-issue-body-warns-not-invoked",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 276,
      "quoted_source_string": "                echo \"> **The Steward was NOT invoked by this issue.** No PAT was available, so it was\"",
      "why": "The warning about a non-triggering token is written into the filed issue itself, not only into the run log, because nobody opens run logs for a job that reported success. The person reading the issue is the one who needs to know it woke nobody.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "NOT invoked by this issue"
    },
    {
      "id": "review-body-fetched-before-author-check",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 190,
      "quoted_source_string": "          # The review this run posted, if any. Fetched BEFORE the author check",
      "why": "Whether a review actually reached the pull request is checked before any branch on who authored the pull request. A lost review strands every author equally, and an author-first ordering exits reporting no action needed without ever discovering that nothing was posted.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "Fetched BEFORE the author check"
    },
    {
      "id": "carveout-detects-workflow-file-edits",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 232,
      "quoted_source_string": "                                --jq '[.[] | select(.filename | startswith(\".github/workflows/\"))",
      "why": "The supply-chain carve-out is recognised by inspecting the changed file list for workflow-directory paths. This is a deliberate security control: a reviewing agent must not execute under continuous integration that the same pull request is editing.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "startswith\\(\"\\.github/workflows/\"\\)"
    },
    {
      "id": "carveout-warning-not-a-lost-review",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 236,
      "quoted_source_string": "              echo \"::warning::Automated review SKIPPED by the supply-chain guard (PR edits workflow files). Not a lost review.\"",
      "why": "A known, explained skip is distinguished from a lost result so the lost-result detector does not fire repeatedly on one benign cause. Without the distinction the detector files an identical issue on every run and gets muted, taking the real detections with it.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "supply-chain guard"
    },
    {
      "id": "carveout-says-so-on-the-pr",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 226,
      "quoted_source_string": "            # the person merging will see it, instead of in a run log nobody opens.",
      "why": "The carve-out is not a pass. It fires precisely on the changes where a second pair of eyes matters most, so it announces itself where the person merging will see it rather than in a run log nobody opens.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "in a run log nobody opens"
    },
    {
      "id": "carveout-deliberately-narrow",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 228,
      "quoted_source_string": "            # Deliberately narrow. Every other \"green run, no comment\" still files an",
      "why": "The carve-out is scoped as narrowly as possible: every other green-run-with-no-comment still escalates. Widening a known-benign exception is the standard way a detector stops detecting, so the narrowness is written down as a constraint rather than left to taste.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "Deliberately narrow"
    },
    {
      "id": "carveout-silences-both-reviewers",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 224,
      "quoted_source_string": "            # reviewers (<ROLE-A> + <ROLE-B>) run this same action, so a trip silences both.",
      "why": "Both reviewer roles run the same underlying runner, so one supply-chain trip silences every reviewer at once. There is no surviving reviewer to defer to, which is exactly why the skip must be announced rather than absorbed.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "A shared execution path means a single guard trip removes the whole reviewing capability, not one of two opinions.",
      "replacement_must_preserve": "A statement that both reviewer roles share the same runner and are therefore both silenced by one trip, so no surviving opinion exists to fall back on.",
      "discharged_in": ".github/workflows/review.yml — the supply-chain carve-out notice states that both reviewers are affected and that the green check means the job exited cleanly, not that anything was reviewed. See tests/harness-guards/semantic-discharges.md row 5."
    },
    {
      "id": "review-escalate-unrecognised-format",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 325,
      "quoted_source_string": "          # the finding marker, on purpose: an unrecognised format then counts",
      "why": "The clean/not-clean decision is keyed on the phrase that means clean, not on the marker that means a finding. An unrecognised output format then counts as findings and gets escalated rather than silently dropped: wrong in the direction of one spurious escalation, never in the direction of another stranded finding. MOVED 2026-08-08: the handoff decision was extracted from .github/workflows/review.yml into tools/review-handoff-decide.sh so it could be exercised directly instead of by carving a step body out of YAML. The pattern is unchanged — the lesson moved home, it did not weaken — and it now sits beside the branch it governs.",
      "expected_in": "tools/review-handoff-decide.sh",
      "pin_kind": "regex",
      "pattern": "unrecognised format"
    },
    {
      "id": "review-clean-phrase-literal",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 329,
      "quoted_source_string": "          if printf '%s' \"$BODY\" | grep -qF '<CLEAN-PHRASE>'; then",
      "why": "The clean-result test is a fixed-string match on one exact phrase the reviewer is required to emit. The phrase itself is tool-specific and will change with the runner, but it must stay a single literal fixed-string match, because a loosened or pattern-based test silently reclassifies findings as clean.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Exactly one literal phrase means clean; everything else, including malformed output, means escalate.",
      "replacement_must_preserve": "A fixed-string (non-regex) match against the template's own clean phrase, with the surrounding comment explaining that keying on the clean phrase rather than the finding marker is what makes unrecognised output escalate.",
      "discharged_in": ".github/workflows/review.yml — `grep -qF 'No issues found'`, keyed on the clean phrase so unrecognised output escalates rather than being dropped. See tests/harness-guards/semantic-discharges.md row 6."
    },
    {
      "id": "dedupe-avoids-search-in-title",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 339,
      "quoted_source_string": "          # `--search \"... in:title\"` is not reliable here: GitHub's search",
      "why": "Server-side title search tokenises the query, so a number in a title is not anchored and a shorter number matches a longer one, while bracketed prefixes are dropped as punctuation. Both directions are harmful: a false match suppresses a real handoff, a false miss files a duplicate that costs another agent run.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "in:title"
    },
    {
      "id": "dedupe-exact-literal-whole-line",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 355,
      "quoted_source_string": "                  | grep -cFx \"$TITLE\" || true)\"",
      "why": "Deduplication is a client-side exact whole-line literal comparison: fixed-string, whole-line, with the no-match exit code absorbed so a strict shell does not turn no-match into a step failure. String equality has neither the false-match nor the false-miss failure mode of tokenised search.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "grep -cFx"
    },
    {
      "id": "reviewer-b-exports-ran-output",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 427,
      "quoted_source_string": "      ran: ${{ steps.gate.outputs.run }}",
      "why": "The optional second reviewer publishes whether it actually ran as a job output, so the downstream comparison can skip cleanly instead of comparing against a review that does not exist. Inferring it from job conclusion would read a clean skip as a successful review.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "ran:[[:space:]]*\\$\\{\\{[[:space:]]*steps\\.gate\\.outputs\\.run"
    },
    {
      "id": "reviewer-b-unset-secret-degrades",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 435,
      "quoted_source_string": "      # An unset secret must degrade to \"one reviewer\", loudly, never to a red",
      "why": "An optional credential that is not configured degrades the pipeline to one reviewer, loudly, and must never turn into a red check on every pull request in the repository. Adding a second opinion may not be able to take the first one down.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "An unset secret must degrade"
    },
    {
      "id": "reviewer-b-gate-warning-one-reviewer",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 441,
      "quoted_source_string": "            echo \"::warning::<CREDENTIAL-SECRET> is not set - skipping the <ROLE-B> second review. This PR got ONE reviewer, not two.\"",
      "why": "The degradation is announced with a warning annotation that states plainly how many reviewers actually ran. A quiet skip leaves a green check that reads as fully reviewed when half the reviewing capacity never executed.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Missing optional credential produces a warning annotation naming the secret and stating the reduced level of assurance, then continues.",
      "replacement_must_preserve": "A warning annotation (not an error, not silence) that names the missing credential and says explicitly that this change got one reviewer rather than two.",
      "discharged_in": null,
      "partial_discharge": "PARTIAL. The credential gate and the degrade-never-fail branch are in .github/workflows/review.yml. NOT landed: the literal 'one reviewer, not two' wording, which belongs in tools/run-agent.sh so it exists in one place, and run-agent.sh is not written yet. Left null on purpose: half a discharge is an open obligation. See tests/harness-guards/semantic-discharges.md row 7."
    },
    {
      "id": "reviewer-b-must-not-read-first-review",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 470,
      "quoted_source_string": "A first review by a different model has already been posted. DO NOT READ IT, and do not read any other existing comment or review on this PR before forming your own findings - fetching them at all defeats the purpose of a second opinion.",
      "why": "The second reviewer is forbidden from reading the first before forming its own findings. Agreement arrived at independently is evidence; agreement copied from the other reviewer is noise, and a checker that can only reach the same answer is not a check at all.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Independence of the second opinion is enforced in the instructions, not merely hoped for.",
      "replacement_must_preserve": "An explicit prohibition on the challenge role reading the judge's review or any other existing comment before forming its own findings, with the reason (copied agreement is worthless) stated.",
      "discharged_in": ".github/workflows/review.yml — reviewer B step comment states the prohibition and the reason: agreement it copied is noise. See tests/harness-guards/semantic-discharges.md row 8."
    },
    {
      "id": "reviewer-marker-html-comment-prefix",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 470,
      "quoted_source_string": "<!-- reviewer:",
      "why": "Each reviewing role self-identifies with an HTML-comment marker as the exact first line of what it posts. The marker prefix is what every downstream consumer keys on, so it must survive genericisation character-for-character even though the role name inside it changes.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "<!-- reviewer:"
    },
    {
      "id": "reviewer-marker-required-first-line",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 470,
      "quoted_source_string": "the FIRST line of the comment you post must be exactly the HTML comment <!-- reviewer: <ROLE-B> -->",
      "why": "The marker is required to be the exact first line, not merely present somewhere. First-line placement is what makes the marker cheap and unambiguous to detect and impossible to produce accidentally in prose.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "A mandatory, machine-readable, first-line role marker on every posted review.",
      "replacement_must_preserve": "An instruction that the first line of the posted comment is exactly the role marker, with the role token replaced by the template's role name, and the requirement described as mandatory rather than preferred.",
      "discharged_in": ".github/workflows/review.yml — both reviewer step comments state the exact first-line marker as a non-optional part of the contract. See tests/harness-guards/semantic-discharges.md row 9."
    },
    {
      "id": "reviewer-marker-same-bot-account",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 470,
      "quoted_source_string": "Both reviews are posted by the same bot account, so without that marker line nothing downstream can tell which review is yours.",
      "why": "Every agent posts from the same bot identity, so author is useless for telling roles apart, and status chatter lands in the same window from the same account. Only an in-body marker distinguishes a review from a progress note or from the other role's review.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Author identity cannot discriminate between agent roles; the marker is the only discriminator, and the reason is recorded next to the requirement.",
      "replacement_must_preserve": "A sentence stating that all roles post from the same account and that without the marker nothing downstream can tell the reviews apart.",
      "discharged_in": ".github/workflows/review.yml — 'Every role posts from the SAME bot account, so nothing downstream can tell two reviews apart by author; the marker is the only discriminator.' See tests/harness-guards/semantic-discharges.md row 10."
    },
    {
      "id": "referee-gated-on-b-having-run",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 484,
      "quoted_source_string": "    if: ${{ !cancelled() && needs.<ROLE-B-JOB>.outputs.ran == 'true' && github.event.pull_request.draft == false }}",
      "why": "The comparison job is skipped when the second reviewer did not run, gated on that reviewer's own published output rather than on its job conclusion. Skipping cleanly is correct; comparing one review against an empty file and reporting it as a comparison is not.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "A dependent aggregation job is gated on an explicit did-it-actually-run output from its dependency, and skips rather than fails when the dependency degraded.",
      "replacement_must_preserve": "The referee job's condition reading the challenge job's ran output, combined with the not-cancelled guard, so that a missing optional reviewer skips the comparison without failing the change.",
      "discharged_in": ".github/workflows/review.yml — the referee's `if:` requires needs.challenge-review.outputs.ran == 'true', so an absent optional credential skips rather than fails. See tests/harness-guards/semantic-discharges.md row 11."
    },
    {
      "id": "collector-paginate-slurp",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 530,
      "quoted_source_string": "--paginate --slurp",
      "why": "Paged collection uses slurp to merge all pages into one document before any filtering. This is the fix for the per-page filter trap, and the two flags must stay adjacent on the fetch itself rather than being replaced by a per-item filter.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "--paginate[[:space:]]+--slurp"
    },
    {
      "id": "collector-per-page-filter-trap",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 525,
      "quoted_source_string": "          # the filter ONCE PER PAGE and prints one JSON array per page, so on a",
      "why": "Combining pagination with a per-item filter applies the filter once per page and emits one array per page, so any take-the-last operation silently returns one result per page instead of one overall. The bug is invisible until a thread passes one page of comments, which is exactly when the collector has to be right.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "ONCE PER PAGE"
    },
    {
      "id": "collector-jq-add-flattens-pages",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 534,
      "quoted_source_string": "            (if (.[0] | type) == \"array\" then add else . end)",
      "why": "The slurped array-of-arrays is flattened back into one flat list before filtering, with a shape test so a single-page response is handled identically to a multi-page one. Filter after flattening, never before.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "then add else \\. end"
    },
    {
      "id": "collector-single-endpoint-must-merge-both",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 530,
      "quoted_source_string": "          gh api \"repos/$REPO/issues/$PR/comments\" --paginate --slurp \\",
      "why": "Top-level conversation comments and inline comments on a code line live on two different endpoints. A collector that reads only one of them can announce that a review is not present while that review has been sitting on the change the whole time, so both endpoints must be read and merged.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Complete comment collection requires both the issue-comments endpoint and the pull-request review-comments endpoint, merged into one list before filtering.",
      "replacement_must_preserve": "Two paginated fetches, one per endpoint, merged before the role-marker filter runs. The source at this revision reads ONLY the issue-comments endpoint, so this is a gap the extraction must close rather than a behaviour it must copy; the discharge check is that the target reads both.",
      "discharged_in": ".github/workflows/review.yml — both collectors read the issue-comments AND pull-request review-comments endpoints, slurp before filtering, merge, filter by the `<!-- reviewer: ... -->` role marker, and sort_by(.created_at) before `last`. The marker filter was MISSING on the first pass and this pin was wrongly recorded as discharged; behaviourally asserted now by tests/harness-guards/review-collector.bats. See tests/harness-guards/semantic-discharges.md row 12."
    },
    {
      "id": "review-split-by-marker-not-ordering",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 542,
      "quoted_source_string": "          # on a marker rather than on ordering means a retry or an extra",
      "why": "Reviews are separated by their role marker, never by ordering such as newest-comment-wins. Ordering breaks the moment a retry, a status note or any unrelated comment lands in the same window from the same account, and it breaks silently by swapping two results rather than by erroring.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "on a marker rather than on ordering"
    },
    {
      "id": "review-selection-must-not-use-exclusion",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 546,
      "quoted_source_string": "          jq -r '[.[] | select((.body | contains(\"<!-- reviewer: <ROLE-B> -->\")) | not)] | last | .body // \"\"' \\",
      "why": "Selecting one role by excluding the other role's marker is selection by negation: anything else the account posted in the window, including status chatter or a retry, is picked up as that role's review. Each role must be selected by its own positive marker.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "semantic-manual",
      "concept": "Positive per-role marker matching, not exclusion of the other role and not recency.",
      "replacement_must_preserve": "Both roles emitting their own marker, and both selections matching a positive marker. The source at this revision selects the first role by exclusion, so the extraction must tighten this rather than copy it.",
      "discharged_in": ".github/workflows/review.yml — VARIANT, tightened: both roles emit their own marker and both are selected by positive match, where the source selected role A by excluding role B's marker. See tests/harness-guards/semantic-discharges.md row 13."
    },
    {
      "id": "referee-one-missing-review-is-a-finding",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 555,
      "quoted_source_string": "          # One review missing is a real finding about the review pipeline, not",
      "why": "When one of the two expected reviews is absent, the comparison step says which one is missing, on the change itself, rather than staying quiet. A green check must never read as reviewed twice when it was reviewed once.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "One review missing is a real finding"
    },
    {
      "id": "referee-posts-via-plain-script-step",
      "source_file": ".github/workflows/<VENDOR-REVIEW-WORKFLOW>.yml",
      "source_line": 595,
      "quoted_source_string": "      # that can quietly post nothing and still go green.",
      "why": "Publishing is done by a plain script step reading a file the agent wrote, not by the agent itself. An agent that decides for itself whether to publish is an agent that can quietly publish nothing and still report success.",
      "expected_in": ".github/workflows/review.yml",
      "pin_kind": "regex",
      "pattern": "quietly post nothing and still go green"
    },
    {
      "id": "steward-workflow-level-concurrency-is-wrong",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 83,
      "quoted_source_string": "# Workflow-level concurrency is evaluated when the RUN is created — before any",
      "why": "Workflow-level concurrency is evaluated when the run is created, before any job condition runs, so an event the workflow deliberately ignores still enters the group, still evicts whatever was pending, and only then skips. The group therefore has to be declared at job level.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "Workflow-level concurrency is evaluated when the RUN is created"
    },
    {
      "id": "steward-gate-runs-before-the-runner",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 115,
      "quoted_source_string": "    # workflow. This gate runs on GitHub's side BEFORE the job reaches our",
      "why": "The trigger event cannot be filtered by comment author or content, so the filtering happens in a job condition that is evaluated before the job reaches a runner. Filtered events then show as instantly skipped instead of occupying the queue.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "BEFORE the job reaches"
    },
    {
      "id": "steward-auto-triage-issues-opened-only",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 123,
      "quoted_source_string": "      (github.event_name == 'issues' && github.event.action == 'opened') ||",
      "why": "Automatic triage fires on newly opened issues only, and that branch carries no mention requirement and no sender requirement. It is the one unconditional path, which is also what makes it usable as a cross-workflow handoff target.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "github\\.event_name == 'issues' && github\\.event\\.action == 'opened'"
    },
    {
      "id": "steward-auto-triage-needs-no-tag",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 119,
      "quoted_source_string": "    # `issues.opened` is the one unconditional case — that is the auto-triage",
      "why": "The comment records that the newly-opened-issue path is deliberately the only unconditional trigger and that reassignment of an existing issue stays gated. Without it, someone tidying the condition adds a mention requirement and the handoff path goes dark.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "the one unconditional case"
    },
    {
      "id": "steward-bot-sender-gate",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 125,
      "quoted_source_string": "github.event.sender.type != 'Bot'",
      "why": "Every comment-driven and review-driven trigger requires that the sender is not a bot. Without it the reviewer's own output wakes the fixer, which pushes, which triggers another review, forever, on a runner that executes one job at a time.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "github\\.event\\.sender\\.type != 'Bot'"
    },
    {
      "id": "steward-review-comment-trigger-bot-gate",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 126,
      "quoted_source_string": "(github.event_name == 'pull_request_review_comment' && github.event.sender.type != 'Bot'",
      "why": "The bot-sender exclusion is applied to the inline-code-comment trigger too, not only to the top-level conversation trigger. A single unguarded event type is enough to reopen the loop, so the guard is repeated on every one of them.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "pull_request_review_comment' && github\\.event\\.sender\\.type != 'Bot'"
    },
    {
      "id": "steward-review-submitted-trigger-bot-gate",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 127,
      "quoted_source_string": "(github.event_name == 'pull_request_review' && github.event.sender.type != 'Bot'",
      "why": "The bot-sender exclusion is applied to the submitted-review trigger as well. Reviews submitted by an automated reviewer are the exact events that would otherwise wake the fixer and close the loop.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "pull_request_review' && github\\.event\\.sender\\.type != 'Bot'"
    },
    {
      "id": "steward-mention-required-on-every-other-trigger",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 125,
      "quoted_source_string": "contains(github.event.comment.body, '@<VENDOR-MENTION>')",
      "why": "Apart from the newly-opened-issue path, every trigger requires an explicit mention in the body as well as a non-bot sender. Both halves are needed: the mention keeps ordinary conversation from invoking the agent, the sender check keeps agents from invoking each other.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "semantic-manual",
      "concept": "Mention-gating on every non-auto-triage trigger, alongside the bot-sender exclusion.",
      "replacement_must_preserve": "A body-contains check against the template's configurable mention token on each comment and review trigger, ANDed with the bot-sender exclusion rather than replacing it.",
      "discharged_in": ".github/workflows/steward.yml — contains(..., vars.AGENT_MENTION || '@agent') ANDed with sender.type != 'Bot' on all three comment and review triggers. See tests/harness-guards/semantic-discharges.md row 14."
    },
    {
      "id": "steward-concurrency-group-per-issue-or-pr",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 137,
      "quoted_source_string": "${{ github.event.issue.number || github.event.pull_request.number }}",
      "why": "The queue is scoped per issue or pull request number, using a fallback because the two event families carry the number in different places. A single global group means a request on one thread silently evicts a pending request on an unrelated thread.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "github\\.event\\.issue\\.number \\|\\| github\\.event\\.pull_request\\.number"
    },
    {
      "id": "steward-cancel-in-progress-false",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 138,
      "quoted_source_string": "      cancel-in-progress: false",
      "why": "A request that is already executing must not be killed by a newer request on the same thread, because the running job is doing real work with side effects. This is the opposite setting from an advisory check and the two must not be unified for consistency.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "cancel-in-progress:[[:space:]]*false"
    },
    {
      "id": "steward-concurrency-at-job-level",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 130,
      "quoted_source_string": "    # skipped job never enters the group and so can never displace a pending",
      "why": "The concurrency group is declared at job level so the job condition is evaluated first: a skipped job never enters the group and therefore can never displace a pending request. Declared at workflow level, the workflow reliably generates its own evictions from events it then ignores.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "skipped job never enters the group"
    },
    {
      "id": "steward-one-running-one-pending",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 133,
      "quoted_source_string": "    # One running + one pending per group is all GitHub offers; a third event on",
      "why": "The platform keeps one running plus exactly one pending run per concurrency group, and a third event evicts the pending one with no run, no comment and no notification. The queue cannot be made deeper, which is why the residual eviction has to be reported instead of prevented.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "One running \\+ one pending per group"
    },
    {
      "id": "steward-agent-invocation-step",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 153,
      "quoted_source_string": "      - uses: <VENDOR-ACTION>@v1",
      "why": "The agent is invoked through one clearly identified step so that everything around it, the gates before and the outcome checks after, stay vendor-independent. Swapping the runner must not require rewriting the guard logic that surrounds it.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "semantic-manual",
      "concept": "A single, replaceable agent-invocation step with all policy expressed outside it.",
      "replacement_must_preserve": "One invocation of the template's generic agent runner script, with the surrounding trigger gates, concurrency group, outcome check and eviction reporter unchanged.",
      "discharged_in": ".github/workflows/steward.yml — one `tools/run-agent.sh steward` step replaces the vendor action block; gates, concurrency, outcome check and eviction reporter are unchanged around it. See tests/harness-guards/semantic-discharges.md row 15."
    },
    {
      "id": "steward-record-job-start-instant",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 151,
      "quoted_source_string": "        run: echo \"iso=$(date -u +%Y-%m-%dT%H:%M:%SZ)\" >> \"$GITHUB_OUTPUT\"",
      "why": "A timestamp is recorded before the agent runs so later steps can distinguish something this run produced from something already present. Without a recorded start instant, any after-the-fact scan either misses new output or counts old output as new.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "date -u \\+%Y-%m-%dT%H:%M:%SZ"
    },
    {
      "id": "steward-prompt-folded-block-scalar",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 165,
      "quoted_source_string": "          # plain scalar, where \" #\" opens a YAML comment: the value was",
      "why": "Multi-line instruction text is written as a folded block scalar because in a plain scalar a space followed by a hash opens a YAML comment, which truncates the value mid-expression and makes the whole file unparseable. The failure appears as zero-job runs named by file path, not as a syntax error at the offending line.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "opens a YAML comment"
    },
    {
      "id": "steward-env-var-indirection-untrusted-input",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 201,
      "quoted_source_string": "      # Env-var indirection also keeps untrusted issue titles out of the shell.",
      "why": "User-supplied titles and bodies are passed through environment variables rather than interpolated into a shell command or a template expression. Interpolation puts attacker-controlled text into the command line, and the same mistake also reintroduces the YAML truncation hazard.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "untrusted issue titles out of the shell"
    },
    {
      "id": "steward-open-pr-only-when-commits-exist",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 218,
      "quoted_source_string": "          if [ -z \"$(git log origin/main..origin/$BRANCH --oneline 2>/dev/null)\" ]; then",
      "why": "A branch is only turned into a pull request when it actually carries commits ahead of the base. The agent reports a branch name whether or not anything was committed, so the branch name alone is not evidence that work happened.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "git log origin/main\\.\\.origin/\\$BRANCH"
    },
    {
      "id": "steward-green-silent-run-unrecoverable",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 260,
      "quoted_source_string": "      # not, because nobody knows to look.",
      "why": "A red run is recoverable because someone re-runs it; a green run that produced nothing is not, because nobody knows to look. That asymmetry is the whole reason a visible-outcome check exists and deliberately fails the job.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "nobody knows to look"
    },
    {
      "id": "steward-visible-outcome-step",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 266,
      "quoted_source_string": "      - name: Require a visible outcome on auto-triage runs",
      "why": "Automatic triage must leave something a human can see. A dedicated step asserts that, rather than trusting the agent to have spoken, because in the automatic path there is no tracking comment for it to update and posting depends on the model choosing to shell out.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "Require a visible outcome"
    },
    {
      "id": "steward-outcome-paginated-comment-scan",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 280,
      "quoted_source_string": "            const comments = await github.paginate(github.rest.issues.listComments, {",
      "why": "The visible-outcome check scans comments with full pagination rather than reading the first page. On a long thread an unpaginated read misses exactly the newest comments, which are the only ones this check is about.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "paginate\\(github\\.rest\\.issues\\.listComments"
    },
    {
      "id": "steward-outcome-window-from-job-start",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 271,
      "quoted_source_string": "          STARTED_AT: ${{ steps.jobstart.outputs.iso }}",
      "why": "The comment scan is bounded by the instant recorded before the agent ran, so a comment that was already on the thread cannot be mistaken for output this run produced. Any comment inside the window counts, including a human reply, because the point is that the thread is not silently unanswered.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "steps\\.jobstart\\.outputs\\.iso"
    },
    {
      "id": "steward-outcome-branch-must-exist-remotely",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 289,
      "quoted_source_string": "            // nothing was committed, so existence on the remote is the real signal —",
      "why": "The second acceptable outcome is a branch that genuinely exists on the remote, checked by querying it, not a branch name reported by the agent. The name is populated even when nothing was committed, so trusting it turns a silent run into a false pass.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "existence on the remote is the real signal"
    },
    {
      "id": "steward-outcome-marker-comment",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 316,
      "quoted_source_string": "                `<!-- steward-no-outcome:${context.runId} -->`,",
      "why": "When neither visible outcome is present the workflow posts a marker comment keyed by run identifier, so the notice is both machine-detectable and non-duplicating. The marker also states that no conclusion about the request should be drawn from the silence.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "<!-- steward-no-outcome:"
    },
    {
      "id": "steward-outcome-deliberate-failure",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 331,
      "quoted_source_string": "            core.setFailed(",
      "why": "After posting the notice the step deliberately fails the run. Leaving it green would preserve the exact condition being detected, a run that looks successful and produced nothing, which is unrecoverable because nobody knows to look.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "core\\.setFailed\\("
    },
    {
      "id": "steward-outcome-not-cancelled-not-always",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 261,
      "quoted_source_string": "      # `!cancelled()` rather than `always()`: a run the operator killed deliberately —",
      "why": "The outcome check runs on not-cancelled rather than always, so a run an operator killed on purpose is not reported as a lost result. It still fires when the agent step fails, which is a genuine silent-outcome case.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "rather than .always\\(\\)"
    },
    {
      "id": "steward-eviction-reporter-exists",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 354,
      "quoted_source_string": "      - name: Report any steward requests that were evicted from the queue",
      "why": "The residual eviction cannot be prevented, so the run that took the slot reports on the ones it displaced. An evicted run executes nothing and therefore cannot report itself; the requester otherwise sees no run, no comment and no notification, indistinguishable from the trigger never working.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "evicted from the queue"
    },
    {
      "id": "steward-eviction-signature-zero-jobs",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 410,
      "quoted_source_string": "                if (jobs.data.total_count > 0) continue;",
      "why": "An evicted run has a precise signature: cancelled with zero jobs ever created, meaning it never reached a runner. A cancellation that left jobs behind was a deliberate human act and must not be reported, or the notice becomes noise.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "total_count > 0"
    },
    {
      "id": "steward-eviction-marker-dedupe",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 420,
      "quoted_source_string": "                const marker = `<!-- steward-eviction:${run.id} -->`;",
      "why": "Eviction notices are deduplicated by a marker keyed on the evicted run identifier, so repeated scans over the same recent window do not post the same notice again. Marker-based dedupe is also what makes a misdirected notice permanent, which is why the target selection has to be right first.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "<!-- steward-eviction:"
    },
    {
      "id": "steward-eviction-filters-pull-requests",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 388,
      "quoted_source_string": "              // .filter(i => !i.pull_request) is load-bearing, not defensive.",
      "why": "The issue listing endpoint also returns pull requests, and a last-wins lookup keyed on title would let a pull request that copied the issue title overwrite the issue. The notice would then land somewhere the requester is not watching, and marker dedupe would make that permanent rather than self-correcting.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "is load-bearing, not defensive"
    },
    {
      "id": "steward-eviction-best-effort",
      "source_file": ".github/workflows/<VENDOR-STEWARD-WORKFLOW>.yml",
      "source_line": 356,
      "quoted_source_string": "        continue-on-error: true",
      "why": "Reporting is best-effort by design: bookkeeping about dropped requests must never be able to fail the run that is doing the actual work. A reporter that can turn a successful run red will be deleted the first time it does.",
      "expected_in": ".github/workflows/steward.yml",
      "pin_kind": "regex",
      "pattern": "continue-on-error:[[:space:]]*true"
    },
    {
      "id": "notifier-issue-is-primary-channel",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 19,
      "quoted_source_string": "# The issue is the PRIMARY channel and needs only GITHUB_TOKEN, which every",
      "why": "The primary alert channel is the one that needs no configuration beyond the token every workflow already has. Any chat or paging integration is additive, so a rotated or missing secret degrades the extra channel rather than the alert itself.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "PRIMARY channel"
    },
    {
      "id": "notifier-must-not-break-on-rotated-secret",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 23,
      "quoted_source_string": "# that breaks when a secret is rotated, or you lose the alert about losing the",
      "why": "The notifier must not be the component that breaks when a credential is rotated, or you lose the alert about losing the alert. Ordering the channels so the zero-configuration one runs first and unconditionally is what enforces that.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "lose the alert about losing the"
    },
    {
      "id": "notifier-callers-must-declare-issues-write",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 26,
      "quoted_source_string": "# CALLERS MUST DECLARE `issues: write` AT WORKFLOW LEVEL.",
      "why": "Every caller of the reusable notifier has to declare the write permission at workflow level. This instruction is addressed to a file that does not exist yet, which is why it lives here in capitals rather than in a document nobody opens when adding a caller.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "CALLERS MUST DECLARE"
    },
    {
      "id": "notifier-permissions-capped-by-caller",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 27,
      "quoted_source_string": "# A reusable workflow's job permissions are capped by the caller's — GITHUB_TOKEN",
      "why": "A reusable workflow can never hold more permission than its caller; token permissions narrow down a call chain and never widen. A caller that declares nothing, or declares only read, turns the notifier's write permission into an escalation request.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "capped by the caller"
    },
    {
      "id": "notifier-startup-failure-kills-whole-caller",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 32,
      "quoted_source_string": "# graph, which surfaces as `startup_failure`: the WHOLE workflow never starts,",
      "why": "The rejected escalation is refused when the run graph is built, so the entire calling workflow never starts, gate job included. The gate therefore goes quiet rather than red, which is the worst possible failure mode for a check: absent, not failing.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "startup_failure"
    },
    {
      "id": "notifier-new-caller-permissions-recipe",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 35,
      "quoted_source_string": "# caller, give it `permissions: {contents: read, issues: write}` and dispatch it",
      "why": "The exact permission block a new caller needs is written out literally, together with the instruction to dispatch it once and confirm the run actually starts. A rule stated only in the abstract gets applied wrongly; a copyable block plus a verification step does not.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "permissions: \\{contents: read, issues: write\\}"
    },
    {
      "id": "notifier-job-declares-issues-write",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 84,
      "quoted_source_string": "      issues: write",
      "why": "The notifier job itself declares the write permission it needs, which is what makes the caller's declaration mandatory and the mismatch detectable. Removing it would make the caller requirement disappear along with the ability to file anything.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "issues:[[:space:]]*write"
    },
    {
      "id": "notifier-issue-title-per-gate",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 100,
      "quoted_source_string": "            const title = `[nightly] ${gate} is failing`;",
      "why": "The tracking issue title is a fixed prefix plus the gate name, which is what makes the open-or-update lookup work. Change the shape of the title and every recurring failure starts a new thread instead of joining the existing one.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "\\[nightly\\] \\$\\{gate\\} is failing"
    },
    {
      "id": "notifier-opens-or-updates-one-issue",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 102,
      "quoted_source_string": "            // One issue per gate, reopened/commented rather than duplicated:",
      "why": "One issue per gate, commented on rather than duplicated. A gate that stays red for a week should be one thread with seven comments, not seven issues nobody closes, because a flood of duplicates is functionally the same as no alert.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "One issue per gate"
    },
    {
      "id": "notifier-what-red-means-input",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 50,
      "quoted_source_string": "      what_red_means:",
      "why": "Each caller must supply, as a required input, one line saying what a human should conclude from this gate being red. An alert that names a failing job without saying what it implies is an alert people learn to skim past.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "regex",
      "pattern": "what_red_means"
    },
    {
      "id": "notifier-secondary-channel-degrades-to-summary",
      "source_file": ".github/workflows/nightly-alert.yml",
      "source_line": 168,
      "quoted_source_string": "              echo \"### <OPS-CHANNEL> ping NOT sent\"",
      "why": "When the optional channel's credentials are absent the job writes a visible note into the run summary and exits successfully, rather than failing or staying quiet. A missing secret must be visible as a missing secret, never mistaken for having no alerts to send.",
      "expected_in": ".github/workflows/nightly-alert.yml",
      "pin_kind": "semantic-manual",
      "concept": "Optional secondary alert channel degrades to an explicit, visible note without failing the notifier or burying the original failure.",
      "replacement_must_preserve": "A step-summary block that states the secondary channel was not used and why, an exit code of zero, and the same treatment for a delivery failure so a notifier fault never masks the gate failure it is reporting.",
      "discharged_in": ".github/workflows/nightly-alert.yml — a missing webhook writes a $GITHUB_STEP_SUMMARY block and exits 0, and a delivery failure gets the same treatment. See tests/harness-guards/semantic-discharges.md row 16."
    },
    {
      "id": "pr-tests-changes-detector-job",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 17,
      "quoted_source_string": "  changes:",
      "why": "A cheap paths-filter job publishes which parts of the tree changed, and everything downstream reads its outputs. Centralising the decision in one job is what allows both jobs and steps to gate on the same answer.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "regex",
      "pattern": "^[[:space:]]*changes:"
    },
    {
      "id": "pr-tests-changes-gates-jobs-and-steps",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 14,
      "quoted_source_string": "  # Detects which parts of the tree changed so unrelated jobs/steps below can",
      "why": "The change detection gates jobs and steps, both. The doubling is what makes a gate whose stack is absent report skipped rather than missing, and what stops a partially-relevant job from paying for toolchains it will not use.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "regex",
      "pattern": "jobs/steps"
    },
    {
      "id": "clean-skip-job-level-gate",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 52,
      "quoted_source_string": "    if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.frontend == 'true'",
      "why": "Jobs are gated by a condition on the change-detector outputs, at job level. A job skipped this way still reports a conclusion, which satisfies a required status check; a job that never triggers reports nothing and blocks merging forever.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "regex",
      "pattern": "^[[:space:]]{4}if:.*needs\\.changes\\.outputs\\."
    },
    {
      "id": "clean-skip-step-level-gate",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 74,
      "quoted_source_string": "        if: needs.changes.outputs.frontend == 'true'",
      "why": "Steps inside a job are gated on the same change-detector outputs as the job itself. Without the step-level half, a job that runs for one stack still installs and executes the toolchain of every other stack, and the clean-skip guarantee only half holds.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "regex",
      "pattern": "^[[:space:]]{8}if:.*needs\\.changes\\.outputs\\."
    },
    {
      "id": "pr-tests-paths-filter-action",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 38,
      "quoted_source_string": "        uses: dorny/paths-filter@v4",
      "why": "Change detection uses a dedicated paths-filter action rather than hand-rolled diff parsing, so the filter semantics match what the platform's own path matching does and there is one place to read the rules.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "regex",
      "pattern": "dorny/paths-filter"
    },
    {
      "id": "pr-tests-changes-outputs-per-stack",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 31,
      "quoted_source_string": "      backend: ${{ steps.filter.outputs.backend }}",
      "why": "The detector re-exports one boolean output per stack area so downstream jobs never reach into the filter step directly. The set of stacks is project-specific; the pattern of one named boolean per area, exported as a job output, is not.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "semantic-manual",
      "concept": "One named per-stack boolean output on the change-detector job, consumed by every gate.",
      "replacement_must_preserve": "A job outputs block re-exporting each filter result under a stable name, with the template's own stack names, so both job-level and step-level gates read from the job rather than from the step.",
      "discharged_in": ".github/workflows/pr-tests.yml — changes.outputs.backend / .frontend consumed at BOTH job level and step level, which is what makes a stack-absent gate report skipped rather than never report. See tests/harness-guards/semantic-discharges.md row 17."
    },
    {
      "id": "pr-tests-runner-label-must-exist",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 22,
      "quoted_source_string": "    # runner offers queues forever and this is a required check. See",
      "why": "Routing a required check to a runner label that no runner offers does not fail; it queues forever, and the change becomes unmergeable. Any switchable runner selection needs this warning written next to it, because the failure looks like slowness rather than misconfiguration.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "regex",
      "pattern": "queues forever and this is a required check"
    },
    {
      "id": "runner-selection-switchable-variable",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 25,
      "quoted_source_string": "    runs-on: ${{ vars.<RUNNER-VAR> || 'ubuntu-latest' }}",
      "why": "Runner selection is a repository variable with a hosted default, so moving work between hosted and self-hosted capacity is a settings change rather than a code change. The default must be the one that always has capacity, so an unset variable never strands a required check.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "semantic-manual",
      "concept": "Runner placement is configuration with a safe hosted fallback, not a hard-coded label.",
      "replacement_must_preserve": "A repository-variable-driven runs-on with a hosted-runner fallback on every job, using the template's own variable name.",
      "discharged_in": ".github/workflows/pr-tests.yml — runs-on: ${{ vars.PR_RUNNER || 'ubuntu-latest' }} on every gate job. Deliberate exception: ci-health-watch.yml and the notifier it calls are pinned hosted. See tests/harness-guards/semantic-discharges.md row 18."
    },
    {
      "id": "pr-tests-required-job-names",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 50,
      "quoted_source_string": "  <FAST-UNIT-JOB>:",
      "why": "Branch protection matches required checks by the reported context string, so job identifiers are an external contract, not an internal detail. Renaming one silently orphans the protection rule, which then waits forever for a context that no longer exists.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "semantic-manual",
      "concept": "Job identifiers double as required-check context strings and are part of the repository's public configuration surface.",
      "replacement_must_preserve": "The frozen two-tier job identifiers, each with a name field character-identical to its identifier, so the reported context is unambiguous and stable.",
      "discharged_in": ".github/workflows/pr-tests.yml — VARIANT, stronger: the twelve frozen ids are in place with NO `name:` key at all, so the context IS the id and the two cannot diverge. See tests/harness-guards/semantic-discharges.md row 19."
    },
    {
      "id": "pr-tests-concurrency-cancel-true",
      "source_file": ".github/workflows/pr-tests.yml",
      "source_line": 11,
      "quoted_source_string": "  cancel-in-progress: true",
      "why": "Superseded runs on the same reference are cancelled, because nobody reads the test result of a commit that has already been replaced. This is only safe where cancelling cannot strand a required check that branch protection is waiting on.",
      "expected_in": ".github/workflows/pr-tests.yml",
      "pin_kind": "regex",
      "pattern": "cancel-in-progress:[[:space:]]*true"
    },
    {
      "id": "mutation-filter-in-job-never-in-trigger",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 21,
      "quoted_source_string": "#     missing. Filter inside the job; never in the trigger.",
      "why": "Scope filtering belongs in a job condition, never in a workflow-level path trigger. The two look equivalent and are not, and the comment is the only thing standing between a future tidy-up and a repository where every unrelated change is permanently unmergeable.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "Filter inside the job; never in the trigger"
    },
    {
      "id": "mutation-required-check-never-reports",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 35,
      "quoted_source_string": "#     ever created. A required check that never reports does not fail; it sits",
      "why": "A required check that never reports does not fail; it waits for a status that can never arrive, and with bypass disabled the change can never merge. Absent is strictly worse than red, because red is visible and absent looks like pending.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "A required check that never reports does not fail"
    },
    {
      "id": "mutation-skipped-satisfies-required-check",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 38,
      "quoted_source_string": "#   * job-level `if:` — the job reports conclusion \"skipped\", which GitHub",
      "why": "A job skipped by its own condition still reports a conclusion, and that conclusion satisfies a required check. This is the mechanism that makes clean-skip work, and it is the difference between a gate that is absent and one that is honestly not applicable.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "reports conclusion \"skipped\""
    },
    {
      "id": "mutation-scope-job-name-must-be-unique",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 55,
      "quoted_source_string": "  # Deliberately NOT named `changes`: pr-tests.yml already publishes a check by",
      "why": "Two workflows must not publish check runs under the same name, because branch protection matches required checks by name string and duplicate names make that matching ambiguous. Check names are a global namespace across the repository, not per file.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "Deliberately NOT named"
    },
    {
      "id": "mutation-branch-protection-matches-by-name",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 56,
      "quoted_source_string": "  # that name, and branch protection matches required checks by name string.",
      "why": "Branch protection matches required checks by name string. That single fact is why job identifiers cannot be renamed casually, why two jobs may not share a name, and why a name field that differs from its identifier is a trap.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "branch protection matches required checks by name string"
    },
    {
      "id": "mutation-min-sample-before-gating",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 22,
      "quoted_source_string": "#   * Requires a MINIMUM number of mutants before enforcing a score. A one-line",
      "why": "A ratio computed over a handful of samples is noise, so the gate only enforces its threshold once the sample is large enough and reports without failing below that. Otherwise one unkillable but harmless case fails an otherwise good change.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "MINIMUM number of mutants before enforcing a score"
    },
    {
      "id": "mutation-min-sample-constant",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 234,
      "quoted_source_string": "          MIN_MUTANTS=20",
      "why": "The minimum sample size is a named constant next to the threshold it protects, so both numbers are visible and adjustable in one place rather than being buried in a formula.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "MIN_MUTANTS="
    },
    {
      "id": "mutation-missing-report-fails",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 221,
      "quoted_source_string": "          # Fail, do not skip. This step only runs when the scope was non-empty",
      "why": "A missing analysis report, at a point where the run should have produced one, fails the job rather than being treated as nothing to check. Treating a missing artefact as a pass is precisely how a gate stops gating while still reporting green.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "Fail, do not skip"
    },
    {
      "id": "mutation-gate-needs-no-extra-toolchain",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 216,
      "quoted_source_string": "        # toolchain the job does not install is how a gate goes quietly",
      "why": "The enforcement step depends on nothing beyond the shell, because it previously relied on an interpreter this job never installs and died with command-not-found after the expensive work had already passed. Depending on a toolchain the job does not install is how a gate goes quietly missing.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "toolchain the job does not install"
    },
    {
      "id": "mutation-scope-script-is-self-tested",
      "source_file": ".github/workflows/pr-mutation.yml",
      "source_line": 155,
      "quoted_source_string": "      # the scope to nothing and report a green mutation check over an empty",
      "why": "The script that decides what the gate examines is tested on every run, before it is trusted. A silently broken scope computation narrows the gate to nothing and reports success over an empty set, which is indistinguishable from passing.",
      "expected_in": ".github/workflows/pr-mutation.yml",
      "pin_kind": "regex",
      "pattern": "report a green mutation check over an empty"
    },
    {
      "id": "validation-workflow-paths-filter-must-be-converted",
      "source_file": ".github/workflows/pr-validation.yml",
      "source_line": 5,
      "quoted_source_string": "    paths:",
      "why": "This workflow still uses a workflow-level path trigger, which is exactly the pattern the diff-scoped gate documents as unusable for a required check: on a non-matching change no check run is ever created, so branch protection waits for a status that never arrives.",
      "expected_in": ".github/workflows/pr-validation.yml",
      "pin_kind": "semantic-manual",
      "concept": "Trigger-level path filtering is replaced by a change-detector job feeding a job-level condition, so the check can be made required.",
      "replacement_must_preserve": "No workflow-level paths block; instead a change-detector job whose output gates each job, so an unrelated change reports skipped rather than never reporting. This is a correction the extraction must make, not a behaviour to copy.",
      "discharged_in": ".github/workflows/pr-validation.yml — no workflow-level paths:; a migration-scope detector job feeds a job-level if:. See tests/harness-guards/semantic-discharges.md row 20."
    },
    {
      "id": "validation-cancel-safe-only-for-non-required",
      "source_file": ".github/workflows/pr-validation.yml",
      "source_line": 22,
      "quoted_source_string": "# is waiting on would leave the PR unmergeable, which is the trap documented in",
      "why": "Cancelling superseded runs is only safe for checks branch protection is not waiting on. Cancelling a required check leaves it reported as cancelled and the change unmergeable, so the decision has to be made per workflow against the current required set.",
      "expected_in": ".github/workflows/pr-validation.yml",
      "pin_kind": "regex",
      "pattern": "would leave the PR unmergeable"
    },
    {
      "id": "validation-required-set-is-named",
      "source_file": ".github/workflows/pr-validation.yml",
      "source_line": 19,
      "quoted_source_string": "# job in this workflow is a required status check (the required set is `<JOB-A>`,",
      "why": "The comment enumerates the current required check set at the point where that set is being relied on. Without the list written down, the next person cannot tell whether the cancellation setting above is still safe, and a wrong guess makes changes unmergeable.",
      "expected_in": ".github/workflows/pr-validation.yml",
      "pin_kind": "semantic-manual",
      "concept": "Any decision that depends on which checks are required names those checks inline, next to the decision.",
      "replacement_must_preserve": "An enumeration of the template's own required check contexts, using the frozen two-tier job identifiers, at the point where the cancellation setting is justified.",
      "discharged_in": ".github/workflows/pr-validation.yml — both tiers' context strings enumerated inline at the cancel-in-progress justification, with the two not-yet-in-the-tree contexts marked. See tests/harness-guards/semantic-discharges.md row 21."
    },
    {
      "id": "validation-error-annotation-on-failure",
      "source_file": ".github/workflows/pr-validation.yml",
      "source_line": 92,
      "quoted_source_string": "          echo \"::error::Flyway validation failed. A migration file is malformed, collides, or has checksum drift. See the flyway output above.\"",
      "why": "Failures emit an error annotation that names the likely causes, so the reason surfaces in the checks user interface rather than only in scrolled-past log output. A gate that fails without saying what it means gets re-run rather than fixed.",
      "expected_in": ".github/workflows/pr-validation.yml",
      "pin_kind": "regex",
      "pattern": "::error::"
    },
    {
      "id": "secret-scan-job-name-differs-from-id",
      "source_file": ".github/workflows/secret-scan.yml",
      "source_line": 21,
      "quoted_source_string": "    name: Scan for secrets with gitleaks",
      "why": "When a job declares a name, the required-check context is that name and not the job identifier. A job whose identifier and name differ is the documented usual failure mode: branch protection is configured against one string while the run reports the other, and the check appears to never report.",
      "expected_in": ".github/workflows/secret-scan.yml",
      "pin_kind": "semantic-manual",
      "concept": "The reported check context is the job name when present, otherwise the identifier; a mismatch between the two silently orphans branch protection.",
      "replacement_must_preserve": "Every job's name field made character-identical to its identifier, so identifier and reported context can never diverge. The source job here has an identifier and a name that differ, which is the defect the template fixes rather than copies.",
      "discharged_in": ".github/workflows/secret-scan.yml — the job is fast-secret-scan with no `name:` key, so id and context can never diverge. Upstream defect fixed. See tests/harness-guards/semantic-discharges.md row 22."
    },
    {
      "id": "secret-scan-stale-temp-on-persistent-runner",
      "source_file": ".github/workflows/secret-scan.yml",
      "source_line": 39,
      "quoted_source_string": "        run: rm -rf \"${TMPDIR:-/tmp}\"/gitleaks*",
      "why": "A persistent runner keeps temporary files between runs, so a leftover file from a previous run makes the next scan fail on a path collision. The cleanup reads the operating system's temporary directory variable rather than assuming a hard-coded path, and runs only where the runner is persistent.",
      "expected_in": ".github/workflows/secret-scan.yml",
      "pin_kind": "regex",
      "pattern": "TMPDIR:-/tmp"
    },
    {
      "id": "secret-scan-full-history-required",
      "source_file": ".github/workflows/secret-scan.yml",
      "source_line": 30,
      "quoted_source_string": "          fetch-depth: 0",
      "why": "Secret scanning needs the full history, not a shallow checkout, because a credential committed earlier and removed later is still in the history and still leaked. A shallow clone makes the scan pass on exactly the repositories that most need it to fail.",
      "expected_in": ".github/workflows/secret-scan.yml",
      "pin_kind": "regex",
      "pattern": "fetch-depth:[[:space:]]*0"
    },
    {
      "id": "ci-health-watchdog-must-not-run-on-the-runner-it-watches",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 9,
      "quoted_source_string": "#     the dead runner can never report the runner dead.",
      "why": "A watchdog scheduled onto the runner it watches goes down with that runner, and its silence is then indistinguishable from health. Pinning it to the hosted runner is the only thing that makes it survive the failure it exists to report. It reads as an inconsistency to anyone standardising runners across the repository, which is exactly why the reason must stay written next to it.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "cannot report that runner dead"
    },
    {
      "id": "ci-health-runs-on-the-hosted-runner-literally",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 56,
      "quoted_source_string": "    runs-on: ubuntu-latest",
      "why": "The runner is a hard-coded hosted label, not one of the switchable runner variables every other job here uses. Routing this job through a runner variable is a one-line change that silently converts the watchdog into part of the thing it watches.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "^ +runs-on: ubuntu-latest"
    },
    {
      "id": "ci-health-could-not-check-is-not-an-incident",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 27,
      "quoted_source_string": "# \"Could not check\" is NOT an incident. A missing/expired PAT, a <VENDOR> 5xx",
      "why": "An expired credential, a provider 5xx or a rate-limit blip mean the watch is BLIND, not that something is wrong. Collapsing those into the alert path pages several times a day about a problem that is not there, and the channel learns to skim past the alert that matters. The accepted cost is stated openly: a silently broken credential stops the paging entirely.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "COULD NOT CHECK\" IS NOT AN INCIDENT"
    },
    {
      "id": "ci-health-two-accumulators-alerts-versus-warnings",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 81,
      "quoted_source_string": "          # Two accumulators, deliberately separate (see header): ALERTS pages",
      "why": "The warning path and the alert path are two separate accumulators on purpose. Merging them is the mechanical way the rule above gets undone: one variable means one severity, and every could-not-check becomes an incident.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "Two accumulators, deliberately separate"
    },
    {
      "id": "ci-health-curl-must-fail-on-http-error",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 72,
      "quoted_source_string": "          # -f makes curl fail (exit 22) on HTTP errors like 403/404 instead",
      "why": "Without curl's fail-on-HTTP-error flag the client exits 0 and hands the JSON error body to the parser, which makes an expired credential indistinguishable from a real finding. The whole warning-versus-alert split above depends on this one flag being present.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "makes curl fail \\(exit 22\\)"
    },
    {
      "id": "ci-health-unarmed-announces-that-it-checked-nothing",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 24,
      "quoted_source_string": "# Actions billing). Until the secret exists the workflow reports a warning",
      "why": "Until its credential exists the watch checks nothing, and it must say so rather than exit green in silence. A check that quietly did nothing looks exactly like a check that found nothing, and an unarmed watchdog that reports green is the most expensive kind of false confidence available.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "::notice title=CI health watch not armed::"
    },
    {
      "id": "ci-health-quota-exhaustion-is-a-known-state-not-a-mystery",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 112,
      "quoted_source_string": "At 100% every ubuntu-latest job dies instantly with no runner assigned. Flip the <RUNNER-VAR> repo variable to '<LABEL>' now (<DEPLOY-DOC> §6) to move PR checks to the <HOST> runner.",
      "why": "When the hosted-minutes allowance runs out, every hosted job dies within seconds with no runner assigned. That is a perfectly predictable state, but with nothing naming it in advance it gets debugged as a mystery infrastructure fault. Alerting at a threshold, with the remediation written into the alert text, turns a cliff into a planned move onto your own runner.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "At 100% every hosted job dies within seconds with no runner assigned"
    },
    {
      "id": "ci-health-failure-is-red-in-the-actions-tab-too",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 145,
      "quoted_source_string": "          # Fail the job so the alert is also loud in the Actions tab.",
      "why": "The job exits non-zero on a real finding rather than only writing a summary, so the finding is red where people already look and not only in whatever channel the notifier reaches. A watchdog whose only output is a step summary is a watchdog nobody reads.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "red in the Actions tab"
    },
    {
      "id": "ci-health-states-its-own-running-cost",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 18,
      "quoted_source_string": "# Cost: this runs on ubuntu-latest (it must — see point 1) 4 times a day at",
      "why": "The watchdog consumes the very allowance it is watching, and the header says how much. An optional scheduled job whose cost is not written down is one an adopter cannot make an informed decision about keeping, and the cadence is the only dial they have.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "regex",
      "pattern": "Cost, stated because an inert watchdog is not a free one"
    },
    {
      "id": "ci-health-finding-must-reach-a-human-outside-the-actions-tab",
      "source_file": ".github/workflows/<CI-HEALTH-WATCH>.yml",
      "source_line": 127,
      "quoted_source_string": "      - name: Send <CHAT-VENDOR> alert",
      "why": "A red run visible only in the Actions tab is most of the way to a check that is not running. The source pushed to one specific chat vendor; what may not change is that a finding here leaves the repository through some pushed channel as well as failing the job.",
      "expected_in": ".github/workflows/ci-health-watch.yml",
      "pin_kind": "semantic-manual",
      "concept": "An operational watchdog's finding reaches a human through the repository's own notifier, not only through a red square in the Actions tab.",
      "replacement_must_preserve": "A dedicated notifier job for this gate alone, calling the shared reusable notifier with its own gate name, severity and runbook, gated on the watch job's result being 'failure' rather than on failure().",
      "discharged_in": ".github/workflows/ci-health-watch.yml — job `notify-ci-health` calls ./.github/workflows/nightly-alert.yml with gate 'CI health — runner liveness and hosted minutes' at S2, gated on needs.watch-ci-health.result == 'failure', and passes runner: 'ubuntu-latest' explicitly. The explicit runner is part of the discharge, not decoration: a called workflow's runs-on cannot be overridden by its caller, so without it the alarm for a dead self-hosted runner queued on the dead runner. See tests/harness-guards/semantic-discharges.md row 23."
    }
  ]
}
